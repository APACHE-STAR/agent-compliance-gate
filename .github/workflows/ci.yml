name: gate-check
on: [push, pull_request]
jobs:
  run-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Run the compliance gate example
        run: python gate.py
